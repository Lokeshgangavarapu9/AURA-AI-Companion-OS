{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "if [ -d \"backend\" ]; then cd backend; fi && npm install --include=dev && npm run build",
    "watchPatterns": ["/backend/**"]
  },
  "deploy": {
    "startCommand": "if [ -d \"backend\" ]; then cd backend; fi && npx prisma migrate deploy && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
